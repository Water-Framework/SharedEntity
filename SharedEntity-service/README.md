# SharedEntity Service Module

Shared Entity Service is a side-service embedded inside micro-service which represents the domain-aggregate.
It is useful when the user wants to expose sharing service across entities.